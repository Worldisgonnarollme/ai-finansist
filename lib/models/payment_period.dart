/// Выбранная пользователем гранулярность отображения налога на главном
/// экране (переключатель "Месяц / Квартал / Год"). Это ТОЛЬКО режим
/// отображения — реальный срок уплаты (AppState.paymentDue) от него не
/// зависит и остаётся привязан к фактическому графику режима (см.
/// TaxDeadlines). [quarter] для ЕСХН означает "полугодие" — см.
/// TaxModeExt.midPeriodLabel в lib/models/tax_mode.dart.
enum PaymentPeriod { month, quarter, year }
