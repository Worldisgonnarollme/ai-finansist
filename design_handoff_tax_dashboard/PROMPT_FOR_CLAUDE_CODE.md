Задача: применить новый визуальный дизайн (light-тема: белый/бежевый/зелёный/рыжий) ко всем экранам Flutter-приложения для расчёта налогов ИП и самозанятых, не потеряв ни одного экрана, виджета, расчёта или бизнес-логики.

Контекст:
- В папке `design_handoff_tax_dashboard/` лежит референс дизайна:
  - `Tax App Prototype.dc.html` — интерактивный HTML-прототип ВСЕХ экранов приложения (онбординг, выбор статуса Самозанятый/ИП, Главная/Дашборд с 3 вариантами оформления — выбран вариант 1, История операций, Выписки, Настройки, Профиль, Налоговый режим и его смена, Подключённые банки и флоу подключения банка, Детали периода, Новая операция). Это ТОЛЬКО референс внешнего вида и структуры экранов, не код для копирования как есть.
  - `README.md` — подробное описание дизайна варианта 1 дашборда (цвета, типографика, отступы, радиусы, копирайт, состояния).
  - `lib/core/theme/*.dart`, `lib/widgets/*.dart`, `lib/screens/dashboard_screen.dart` — уже готовый Dart-код для экрана «Главная» (токены + виджеты TaxSummaryCard/MetricChip/WarningBanner/AdviceCard/TransactionTile), написанный под структуру моего проекта (`lib/core/theme`, `lib/widgets`, `lib/screens`).

Что нужно сделать:
1. Изучи мой текущий Flutter-проект: `lib/core/theme`, `lib/screens`, `lib/widgets`, `lib/features` — весь список экранов, виджетов, роутов и, самое главное, всю логику расчёта налога (формулы НДФЛ/НДС/УСН/патент/НПД, работу с транзакциями, банками, выписками, состояниями и т.д.).
2. Примени новый визуальный стиль (токены из `design_handoff_tax_dashboard/lib/core/theme/`) ко ВСЕМ экранам проекта, а не только к дашборду — используй HTML-прототип и README как эталон внешнего вида для каждого экрана. Экран «Главная» бери с вариантом оформления 1 (градиентная hero-карточка), это уже реализовано в приложенном Dart-коде — используй его как основу/пример паттерна для остальных экранов.
3. Если в HTML-прототипе есть экран, виджет или состояние, которого нет в текущем коде проекта — добавь его (создай новый Dart-файл/виджет/роут), не удаляя ничего из уже реализованного.
4. Если в текущем проекте есть экраны, виджеты или функциональность, которых нет в HTML-прототипе (например, доп. валидации, кейсы ошибок, специфичные для реального продукта фичи) — сохрани их как есть, просто оформи в новой палитре/типографике по аналогии с остальными экранами.
5. КРИТИЧЕСКИ ВАЖНО: не удаляй и не упрощай ни одной уже реализованной функции, расчёта, формулы или бизнес-правила. Меняется только визуальный слой (цвета, шрифты, отступы, радиусы, форма компонентов) — вся логика, состояния, навигация, валидации и расчёты должны продолжать работать точно так же, как сейчас.
6. Один hard-coded градиент в hero-карточке — единственное исключение, где используется несколько акцентных цветов сразу; для всех остальных элементов используй один акцент (зелёный) и рыжий только для предупреждений/срочности — без изобретения новых цветов вне палитры.
7. Замени старую тёмную тему (`ThemeMode.dark`, `Colors.*`/`Color(0xFF...)` вне `AppColors`) на новую light-тему из `design_handoff_tax_dashboard/lib/core/theme/app_colors.dart` — включая ранее не переведённые на дизайн-систему банковские экраны (`bank_selection_screen.dart`, `bank_consent_screen.dart`, `bank_loading_screen.dart`), которые должны выглядеть так же, как остальное приложение.
8. В конце — покажи список изменённых/добавленных файлов и явно перечисли, какие расчёты/формулы/бизнес-правила ты проверил и не тронул.
