name: ai_financial_agent
description: "AI Financial agent — automatic tax accounting"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  intl: ^0.20.2
  http: ^1.2.2
  file_picker: ^8.1.2
  csv: ^6.0.0
  uuid: ^4.5.1
  syncfusion_flutter_pdf: ^34.1.30
  fl_chart: ^1.2.0
  google_fonts: ^8.1.0
  flutter_animate: ^4.5.2
  json_schema: ^5.2.2
  firebase_core: ^4.11.0
  firebase_auth: ^6.5.4
  google_sign_in: ^7.2.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - opa/tax_policy_input.schema.json
    - assets/images/noise.png
