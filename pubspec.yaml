name: ai_finansist
description: "AI-Финансист — автоматический учёт налогов"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  intl: ^0.19.0
  http: ^1.2.2
  file_picker: ^8.1.2
  csv: ^6.0.0
  uuid: ^4.5.1
  syncfusion_flutter_pdf: ^33.2.6
  fl_chart: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
